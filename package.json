{
  "name": "nodelinux",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "trace": "node --nomemory-reducer --trace-gc --trace-opt --prepare-always-opt --trace-opt-stats --trace-deopt --always-opt --nooptimize-for-size app.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "node-hid": "^2.1.1",
    "robotjs": "^0.6.0",
    "ws": "^8.5.0"
  }
}
